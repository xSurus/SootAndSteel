namespace Gamelab.Map
{
    // Src GameplayConfig defaults with the gameplay.json overrides applied (trainHeight is 5 in
    // the json, 6 in the C# default). Screen size is the Src game window.
    public static class TrainTuning
    {
        public const int Width = 10;
        public const int Height = 5;
        public const int TileSize = 80;
        public const int ScreenWidth = 1920;
        public const int ScreenHeight = 1080;
    }
}
